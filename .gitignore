# dependencies
node_modules/

# cached API responses
.cache/

# fitted models and fetched market data
model.json
*.model.json
sample.csv
candles.csv
*.candles.csv

# secrets
.env
.env.*
*.pem
*.key

# build output
dist/
build/
*.tsbuildinfo

# os / editor
.DS_Store
*.log

# sweep output: fitted models and the ranked table
models/

# panel / spa caches (regenerate with src/panel.ts, src/spa.ts)
models/panel-*.json
models/spa-series.json
